# cpu_stress_test
A small suite of C command-line utilities for stress-testing CPUs, reading CPU temperature, identifying the processor, and plotting temperature logs — plus helper scripts for checking dependencies and running a security audit.
